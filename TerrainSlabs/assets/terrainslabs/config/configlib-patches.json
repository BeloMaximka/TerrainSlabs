{
  "version": 1,
  "settings": [
    {
      "code": "smooth_mode",
      "type": "integer",
      "ingui": "World generation mode",
      "comment": "0 - disable, 1 - smooth only surface (cheap), 2 - smooth whole chunk (more expensive, but affects caves)",
      "default": 2,
      "range": {
        "min": 0,
        "max": 2,
        "step": 1
    }
    }
  ]
}
